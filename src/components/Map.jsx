import React from 'react';
import { whithGoogleMap, LoadScript, whithScriptjs, GoogleMap  } from '@react-google-maps/api'
const Map = (props) => {
    return (
        <div>
            
        </div>
    );
}

export default Map;
